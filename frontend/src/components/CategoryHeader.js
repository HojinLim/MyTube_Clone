import React, { useState } from "react"
import { AppBar } from "@mui/material"
const CategoryHeader = () => {
  return (
    <AppBar position="fixed" style={{ marginTop: "50px" }}>
      gggggggg
    </AppBar>
  )
}

export default CategoryHeader
