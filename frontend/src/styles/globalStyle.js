import { Margin } from "@mui/icons-material"

const flex_space_between = {
  display: "flex",
  justifyContent: "space-between",
}
const flex_column = {
  display: "flex",
  flexDirection: "column",
  textAlign: "center",
}

export { flex_space_between, flex_column }
